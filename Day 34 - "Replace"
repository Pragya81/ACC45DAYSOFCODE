#include<cstring>
void replaceCharacter(char input[], char c1, char c2) 
{
    int length;
    length = strlen(input);
    for(int i = 0; i<length; i++)
    {
        if(input[i] == c1)
        {
            input[i] = c2;
        }
    }
}
